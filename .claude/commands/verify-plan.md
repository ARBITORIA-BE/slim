---
description: 현재 PLAN.md 진행 상태와 다음 미완료 항목을 보고
---

PLAN.md를 읽고 다음 형식으로 한 번에 보고한다 (3줄 이내):

```
📊 진행: ${완료}/${총} (${%}) · 🔄 ${진행중} · 🚫 ${차단}
▶️  다음: PLAN ${id} ${제목}
🎯 현재 페이즈: ${페이즈명} (${완료}/${총})
```

추가로 `pnpm harness:plan` 실행 결과를 한 줄로 요약. 정합성 깨졌으면 어느 룰인지 명시.
