node_modules/
.next/
.env.local
.env.*.local
.claude/.baseline/
.claude/settings.local.json
docs/.bkit-memory.json
docs/.pdca-status.json
public/reports/bias/*.json
*.log
scripts/tmp-*.mjs
scripts/tmp-*.ts
.DS_Store

# TypeScript incremental build cache (regenerated per build)
tsconfig.tsbuildinfo

# Playwright 임시 결과 + e2e 스크린샷 (매 실행마다 갱신 — git churn 회피)
test-results/
playwright-report/
e2e/screenshots/

# Claude Code 런타임 아티팩트 (ScheduleWakeup 락 등 — 세션마다 생성)
.claude/scheduled_tasks.lock

# Claude Code agent worktrees (각 에이전트 isolation copy — main trunk 영향 0)
.claude/worktrees/
.gstack/
